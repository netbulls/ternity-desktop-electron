{
  "name": "ternity-desktop",
  "version": "0.1.0-10-geb54708",
  "description": "Ternity Desktop — system tray companion for time tracking",
  "author": {
    "name": "Netbulls",
    "email": "dev@netbulls.io"
  },
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "version-inject": "tsx scripts/version-inject.ts",
    "build": "pnpm version-inject && electron-vite build",
    "build:mac": "pnpm version-inject && electron-vite build && electron-builder --mac --config electron-builder.yml && bash scripts/build-dmg.sh",
    "build:mac:arm64": "pnpm version-inject && electron-vite build && electron-builder --mac --arm64 --config electron-builder.yml && bash scripts/build-dmg.sh arm64",
    "build:mac:x64": "pnpm version-inject && electron-vite build && electron-builder --mac --x64 --config electron-builder.yml && bash scripts/build-dmg.sh x64",
    "build:linux:x64": "pnpm version-inject && electron-vite build && electron-builder --linux --x64 --config electron-builder.yml",
    "build:linux:arm64": "pnpm version-inject && electron-vite build && electron-builder --linux --arm64 --config electron-builder.yml",
    "type-check": "tsc --noEmit",
    "format": "prettier --write .",
    "register-logto-app": "tsx scripts/register-logto-app.ts"
  },
  "dependencies": {
    "clsx": "^2.1.0",
    "lucide-react": "^0.474.0",
    "motion": "^12.34.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^3.0.0"
  },
  "devDependencies": {
    "@electron-toolkit/preload": "^3.0.0",
    "@electron-toolkit/utils": "^4.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.0",
    "electron": "^34.0.0",
    "electron-builder": "^25.0.0",
    "electron-vite": "^3.0.0",
    "postcss": "^8.4.0",
    "prettier": "^3.0.0",
    "tailwindcss": "^3.4.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild"
    ]
  }
}
